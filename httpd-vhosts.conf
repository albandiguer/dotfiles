# Virtual Hosts
#
# Required modules: mod_log_config

# If you want to maintain multiple domains/hostnames on your
# machine you can setup VirtualHost containers for them. Most configurations
# use only name-based virtual hosts so the server doesn't need to worry about
# IP addresses. This is indicated by the asterisks in the directives below.
#
# Please see the documentation at
# <URL:http://httpd.apache.org/docs/2.4/vhosts/>
# for further details before you try to setup virtual hosts.
#
# You may use the command line option '-S' to verify your virtual host
# configuration.

#
# VirtualHost example:
# Almost any Apache directive may go into a VirtualHost container.
# The first VirtualHost section is used for all requests that do not
# match a ServerName or ServerAlias in any <VirtualHost> block.
#

<VirtualHost *:80>
    ServerName tbb.dev
    ProxyPreserveHost On
    ProxyPass / http://127.0.0.1:3000/ retry=0 timeout=180
    ProxyPassReverse / http://127.0.0.1:3000/
</VirtualHost>


<VirtualHost *:80>
    ServerName modern.dev
    ProxyPreserveHost On
    ProxyPass / http://127.0.0.1:8080/ retry=0 timeout=5
    ProxyPassReverse / http://127.0.0.1:8080/
</VirtualHost>


<VirtualHost *:80>
    ServerName marinas.dev
    ProxyPreserveHost On
    ProxyPass / http://127.0.0.1:8080/ retry=0 timeout=5
    ProxyPassReverse / http://127.0.0.1:8080/
</VirtualHost>

<VirtualHost *:80>
    ServerName nexus.dev
    ProxyPreserveHost On
    ProxyPass / http://127.0.0.1:8080/ retry=0 timeout=5
    ProxyPassReverse / http://127.0.0.1:8080/
</VirtualHost>

<VirtualHost *:80>
    ServerName ccc-beef.dev
    ProxyPreserveHost On
    ProxyPass / http://127.0.0.1:3003/ retry=0 timeout=5
    ProxyPassReverse / http://127.0.0.1:3003/
</VirtualHost>

<VirtualHost *:80>
    ServerName ccc-fe.dev
    ProxyPreserveHost On
    ProxyPass / http://127.0.0.1:3004/ retry=0 timeout=5
    ProxyPassReverse / http://127.0.0.1:3004/
</VirtualHost>

<VirtualHost *:80>
    ServerName smil.dev
    ProxyPreserveHost On
    ProxyPass / http://127.0.0.1:5317/ retry=0 timeout=5
    ProxyPassReverse / http://127.0.0.1:5317/
</VirtualHost>
