Follow Nix flake/home-manager patterns: each program module uses `{ pkgs, ... }: { programs.<name> = { enable = true; ... }; }` and is imported through home/users/albandiguer/home.nix. Separate system-level config in darwin/macbook.nix from user-level in home/. Default to ASCII, minimal comments, clear explicit code per repository instructions. Preserve Fish abbreviations, Mise-managed versions, and existing design patterns.